class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        sum_list = list()
        sum = 0
        for i in range(len(nums)):
            sum = sum + nums[i]
            sum_list.append(sum)

        return sum_list
